# edit message schema, to update this, better to copy from send message schema and delete the redirect portion
$id: nodetion/extensions/discord/schemas/message
type: object
properties:
  message:
    type: object
    properties:
      content:
        type: string
      components:
        type: array
        items:
          $ref: nodetion/extensions/discord/schemas/messageComponent    
      embeds:
        type: array
        items:
          $ref: nodetion/extensions/discord/schemas/embed
      attachments:
        type: array
        items: 
          type: string
      stickers:
        type: array
        items: 
          type: integer
      poll:
        $ref: nodetion/extensions/discord/schemas/poll
    unevaluatedProperties: false
unevaluatedProperties: false